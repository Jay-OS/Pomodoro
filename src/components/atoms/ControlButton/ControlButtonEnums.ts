export enum controlButtonType {
    PRIMARY = 'controlPrimary',
    SECONDARY = 'controlSecondary',
    REVERSED = 'controlReversed',
}

export enum controlButtonSize {
    LARGE = 'controlLarge',
    MEDIUM = 'controlMedium',
    SMALL = 'controlSmall',
}
